# LEGACY FILE - This file is kept for reference only.
# The project now uses uv and pyproject.toml as the single source of truth.
# Use `uv sync --dev` instead of `pip install -r requirements-dev.txt`
# See MIGRATION_UV.md for migration instructions.

# Development dependencies
-r requirements.txt

# Testing
pytest>=7.0.0
pytest-cov>=3.0.0

# Code quality
black>=22.0.0
flake8>=4.0.0
mypy>=0.950

# Documentation
sphinx>=4.0.0
sphinx-rtd-theme>=1.0.0
