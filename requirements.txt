# LEGACY FILE - This file is kept for reference only.
# The project now uses uv and pyproject.toml as the single source of truth.
# Use `uv sync` instead of `pip install -r requirements.txt`
# See MIGRATION_UV.md for migration instructions.

numpy>=1.20.0
scipy>=1.7.0
matplotlib>=3.3.0
pandas>=1.3.0
scikit-learn>=1.0.0

# Optional dependencies for Phase 1 features
# Data I/O
rasterio>=1.2.0 # GeoTIFF support
netCDF4>=1.5.0 # NetCDF support
geopandas>=0.10.0 # GeoJSON and spatial data support
openpyxl>=3.0.0 # Excel support

# ML (already covered by scikit-learn, but optional extras)
xgboost>=1.5.0 # For XGBoost kriging

# Phase 2 features
joblib>=1.0.0 # Parallel processing
plotly>=5.0.0 # Interactive visualization

# Phase 3 features
fastapi>=0.100.0 # Web API
uvicorn>=0.23.0 # ASGI server
pydantic>=2.0.0 # Data validation
